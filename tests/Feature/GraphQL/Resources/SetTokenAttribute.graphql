mutation SetTokenAttribute(
  $collectionId: BigInt!
  $tokenId: EncodableTokenIdInput!
  $key: String!
  $value: String!
) {
  SetTokenAttribute(
    collectionId: $collectionId
    tokenId: $tokenId
    key: $key
    value: $value
  ) {
    id
    transactionId
    transactionHash
    method
    state
    encodedData
    wallet {
      account {
        publicKey
        address
      }
    }
  }
}

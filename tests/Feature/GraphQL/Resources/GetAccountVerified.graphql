query GetAccountVerified(
    $verificationId: String
    $account: String
) {
    GetAccountVerified(
        verificationId: $verificationId
        account: $account
    ) {
        verified
        account {
            publicKey
            address
        }
    }
}
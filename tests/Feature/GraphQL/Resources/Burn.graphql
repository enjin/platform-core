mutation Burn(
  $collectionId: BigInt!
  $params: BurnParamsInput!
  $skipValidation: Boolean
) {
  Burn(
    collectionId: $collectionId
    params: $params
    skipValidation: $skipValidation
  ) {
    id
    transactionId
    transactionHash
    method
    state
    encodedData
    wallet {
      account {
        publicKey
        address
      }
    }
  }
}

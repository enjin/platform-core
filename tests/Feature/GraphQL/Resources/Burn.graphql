mutation Burn(
  $collectionId: BigInt!
  $params: BurnParamsInput!
  $skipValidation: Boolean
  $simulate: Boolean
) {
  Burn(
    collectionId: $collectionId
    params: $params
    skipValidation: $skipValidation
    simulate: $simulate
  ) {
    id
    transactionId
    transactionHash
    method
    state
    encodedData
    fee
    deposit
    wallet {
      account {
        publicKey
        address
      }
    }
  }
}

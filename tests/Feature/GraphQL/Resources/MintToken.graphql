mutation MintToken(
  $recipient: String!
  $collectionId: BigInt!
  $params: MintTokenParams!
  $skipValidation: Boolean
) {
  MintToken(
    recipient: $recipient
    collectionId: $collectionId
    params: $params
    skipValidation: $skipValidation
  ) {
    id
    transactionId
    transactionHash
    method
    state
    encodedData
    wallet {
      account {
        publicKey
        address
      }
    }
  }
}
